# 一生一芯·F5·学习记录

## 只有一条指令的`SCPU`(含`Li`指令)

指令周期：

1. 取指(fetch): 根据当前`pc`, 在存储器中找到一条指令
2. 译码(decode): 看这条指令具体是什么指令, 操作数是哪些
   - 以`li`指令为例, 操作数需要看立即数是多少, 需要写入哪个目的寄存器
3. 执行(execute): 对操作数进行处理, 必要时更新指定的目的寄存器
4. 更新`pc`: 让`pc`指向下一条指令

### 取指

取指即根据pc的值，从指令集存储器中取出对应指令的过程。

1. `pc`寄存器，存放`pc`的值
2. 指令集存储器，要求可以寻址、可读、(可写)
   - 数据选择器加输入/寄存器实现
   

### 译码

根据取出的指令，进行位抽取操作，根据指令类型设计对应逻辑电路

- 处理指令类型，并决定指令对应的数据该如何处理、流向何处

### 执行

执行指令包含多种行为，首先是存储数据，因此需要支持写入，即`RAM`。`RAM`的基本结构包含：寄存器，译码器、逻辑门。与门的作用是链接写使能信号，必要时屏蔽输入

- 这里使用三输入与门的原因是为了方便调试，重置与门的状态，以便其对电路状态造成影响

![image-20251005204459069](C:\Users\Sorab\AppData\Roaming\Typora\typora-user-images\image-20251005204459069.png)

### 更新`PC`

更新`PC`值即对寄存器数值加`1`即可，可以使用计数器实现，也可以使用加法器实现。但往往并不是一直顺序执行下去，因此需要数据选择器处理其他需要转跳`PC`的情况。

![image-20251005205211863](C:\Users\Sorab\AppData\Roaming\Typora\typora-user-images\image-20251005205211863.png)

## 添加`add`指令

接下来考虑如何实现`add`指令. 由于对所有指令来说, 取指和更新PC这两个步骤的行为都一样, 因此可以直接复用之前实现的取指逻辑.

对于译码, 由于`add`指令的行为和`li`指令不同, 因此有必要识别当前取出的是什么指令. 为了实现这一点, 需要检查指令的`opcode`字段, 如果是`00`, 则是`add`指令, 如果是`10`, 则是`li`指令. 要识别输入的取值, 最适合的电路就是译码器! 由于`opcode`只有2位, 我们可以使用一个2-4译码器, 它输出的独热码可以指示当前指令属于何种指令, 这样的译码器称为指令译码器. 这组独热码通常作为控制信号, 用于控制部分电路如何工作, 从而支持不同指令的功能.

考虑完操作码之后, 还需要考虑操作数. 和`li`指令相比, `add`指令除了需要写入`rd`, 还需要读出`rs1`和`rs2`作为源操作数, 需要同时通过两个读端口和一个写端口访问GPR. 因此, 我们还需要为GPR添加两个读口, 添加后, GPR模块应至少包含如下端口信号:

- 第1个读端口: `raddr1`(读地址), `rdata1`(读数据)
- 第2个读端口: `raddr2`, `rdata2`
- 写端口: `waddr`(写地址), `wdata`(写数据), `wen`(写使能), `clk`(时钟)

读出源操作数后, `add`指令的行为是将两数相加, 然后将加法结果写入`rd`寄存器. 前者可以通过加法器实现, 但后者写入GPR时, GPR的`wdata`端口已经被`li`指令的结果占用. 考虑到指令`opcode`编码的唯一性, 一条指令不可能既是`li`指令又是`add`指令, 因此可以根据指令的类别对写入GPR的数据进行选择: 如果当前指令是加法指令, 就选择加法器的结果, 否则选择`li`指令的立即数. 可以通过多路选择器实现这样的选择功能, 将写入GPR的两个数据来源作为多路选择器的数据端, 然后让译码时独热码中合适的信号连接到多路选择器的选择端, 来控制多路选择器按照指令类型选出正确的数据, 即可实现上述功能.

## 添加`benr0`指令

`benr0`(`Branch if Not Equal r0`)：如果执行这条指令的时候`R[rs2]`与`R[0]`不相等, 则将PC寄存器更新为`addr`, 即让PC指向`addr`处的指令. 

为了识别`bner0`指令, binary to BCD. 至于操作数, 除了指令中的`rs2`和`addr`, 还有一个隐含的`R[0]`. 由于`bner0`指令中`rs2`字段的位置和`add`指令中`rs2`字段的位置一样, 因此可以复用`add`指令中读出`rs2`寄存器的逻辑. 但`bner0`还需要读出`R[0]`, 因此可以把`0`作为GPR的`raddr1`端口的输入. 不过这个端口已经被`add`指令的`rs1`占用, 但也同样可以通过多路选择器的解决问题.

![image-20251005210116329](C:\Users\Sorab\AppData\Roaming\Typora\typora-user-images\image-20251005210116329.png)

读出源操作数后, `bner0`指令需要比较两数是否相等, 这可以通过比较器来实现. 若比较结果不相等, 需要将PC更新为`addr`字段. 换句话说, 只有当前指令为`bner0`指令, 且比较结果不相等, 才将PC更新为`addr`字段, 其余情况应将PC更新为PC加1. 同样地, 我们可以借助多路选择器对PC寄存器的输入端进行选择.

![image-20251005210027032](C:\Users\Sorab\AppData\Roaming\Typora\typora-user-images\image-20251005210027032.png)

最后, `bner0`指令不会写入GPR, 因此需要将GPR的`wen`置为无效.

![image-20251005205912342](C:\Users\Sorab\AppData\Roaming\Typora\typora-user-images\image-20251005205912342.png)

## 添加`out rs`指令

将任意输出r[s]的数值通过两个7段数码管显示，指令含义需自定义。寄存器输出类型为`8421bcd`码，需要先将其转换为`BCD`码，这里采用`logsim`中自带的`binary to BCD`模块进行转换，数码管译码电路使用两个4*8Rom完成。

其次，处理`r[s]`字段需要另外新增两个数据选择器，一个用与选择数据通路，一个用于指令译码。

| 指令类型[7:6] | 空[5:2] | 源寄存器[1:0] |
| :-----------: | :-----: | :-----------: |
|      01       |  0000   |     r[s]      |

电路实现如下：

![image-20251006192105352](C:\Users\Sorab\AppData\Roaming\Typora\typora-user-images\image-20251006192105352.png)

## 总结

1. 实现能处理3条指令的`scpu`，每添加一条指令时，电路复杂度指数级上升，需要处理新指令的逻辑电路与旧指令逻辑电路冲突的部分(mux)，需要考虑执行新指令时旧指令电路是否需要屏蔽，如执行`bner0`时，需要将GPR的`wen`置为无效，否则电路计算将会出错。
2. 总电路图如下，功能实现正常，计算结果为55

![image-20251006192044500](C:\Users\Sorab\AppData\Roaming\Typora\typora-user-images\image-20251006192044500.png)

